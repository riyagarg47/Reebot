To create a vitual environemnt (similar to how node create node_modules in a project) = `python3 -m venv .myenv`

Activating the virtual environment in the terminal = `source .myenv/bin/activate`

Installing a library = `pip3 install pymupdf4llm`

To run the python script = `python3 test_parser.py`
